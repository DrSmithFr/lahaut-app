import { FlyLocationPipe } from './fly-location.pipe';

describe('FlyLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new FlyLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
